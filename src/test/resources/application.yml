spring:
  datasource:
    platform: postgres
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: username
    password: password
  jpa:
    database: POSTGRESQL
    generate-ddl: true
  http:
    converters:
      preferred-json-mapper: gson